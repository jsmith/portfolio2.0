{
  "name": "portfolio2.0",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint",
    "deploy": "git clone git@github.com:jsmith/jsmith.github.io repo && rm -rf repo/assets && npm run build && cp -r dist/* repo && rm -r dist && cd repo && git add . && git commit -m \"release\" && git push && cd .. && rm -rf repo"
  },
  "dependencies": {
    "@vue/composition-api": "^0.3.2",
    "core-js": "^3.3.2",
    "fp-ts": "^2.5.3",
    "io-ts": "^2.1.2",
    "tailwindcss": "^1.2.0",
    "vue": "^2.6.10",
    "vue-material-design-icons": "^4.1.0"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "^4.0.0",
    "@vue/cli-plugin-typescript": "^4.0.0",
    "@vue/cli-service": "^4.0.0",
    "sass": "^1.19.0",
    "sass-loader": "^8.0.0",
    "typescript": "~3.5.3",
    "vue-template-compiler": "^2.6.10"
  }
}
